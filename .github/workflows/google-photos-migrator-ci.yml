name: Google Photos Migrator CI

on:
  push:
    branches:
      - main
      - feature/google-photos-migrator
  pull_request:
    paths:
      - 'src/**'
      - 'pom.xml'
      - 'Dockerfile'
      - 'deployment/**'
      - '.github/workflows/google-photos-migrator-ci.yml'
      - '.github/workflows/google-photos-migrator-cloudrun.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Validate deployment bootstrap syntax
        run: bash -n deployment/bootstrap-gcp.sh
      - name: Maven verify
        run: mvn --batch-mode --update-snapshots verify
